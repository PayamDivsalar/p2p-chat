# راهنمای پروژه چت  (P2P)

## 🎯 اجزای اصلی پروژه

پروژه از **دو سرویس اصلی** تشکیل شده:

### 1️⃣ سرویس STUN Server (سرور مدیریت آدرس‌ها)

**وظیفه**: مدیریت لیست کاربران و آدرس‌های شبکه آن‌ها

**چه کاری انجام می‌دهد؟**
- وقتی یک کاربر وارد سیستم می‌شود، اطلاعاتش (نام کاربری، آدرس IP، پورت) را ثبت می‌کند
- لیست تمام کاربران آنلاین را نگهداری می‌کند
- به کاربران اجازه می‌دهد لیست دیگر کاربران را ببینند
- اطلاعات یک کاربر خاص را در اختیار دیگران قرار می‌دهد (برای برقراری ارتباط)

**تکنولوژی‌های استفاده شده:**
- **FastAPI**: یک فریمورک سریع و مدرن Python برای ساخت API
- **Redis**: یک پایگاه داده سریع که در حافظه کار می‌کند برای ذخیره اطلاعات کاربران
- **Docker**: برای اجرای آسان Redis

**پورت پیش‌فرض**: 8000

### 2️⃣ سرویس Peer Node (گره همتا)

**وظیفه**: برقراری ارتباط مستقیم بین کاربران و ارسال پیام/فایل

**چه کاری انجام می‌دهد؟**
- با سرور STUN ارتباط برقرار می‌کند و خودش را ثبت می‌کند
- لیست کاربران دیگر را از سرور STUN دریافت می‌کند
- **به عنوان سرور**: منتظر می‌ماند تا کاربران دیگر به آن متصل شوند
- **به عنوان کلاینت**: به کاربران دیگر متصل می‌شود
- پیام‌های متنی و فایل‌ها را مستقیماً (بدون واسطه) ارسال و دریافت می‌کند

**تکنولوژی‌های استفاده شده:**
- **TCP Socket**: برای ارتباط مستقیم بین کاربران
- **HTTP Client**: برای ارتباط با سرور STUN
- **Threading**: برای مدیریت چندین اتصال همزمان

**پورت پیش‌فرض**: 9000 (قابل تغییر برای هر کاربر)

## 🔄 چطور کار می‌کند؟

### مرحله 1: راه‌اندازی اولیه

```
1. Redis اجرا می‌شود (با Docker Compose)
   └─> پایگاه داده برای STUN Server آماده می‌شود

2. STUN Server راه‌اندازی می‌شود
   └─> به Redis متصل می‌شود
   └─> منتظر درخواست‌های کاربران می‌ماند
```

### مرحله 2: ورود کاربر به سیستم

```
کاربر 1 (علی):
  1. برنامه Peer Node را اجرا می‌کند
  2. به STUN Server متصل می‌شود
  3. اطلاعاتش ثبت می‌شود: { نام: "علی", آدرس: "192.168.1.10", پورت: 9000 }
  4. سرور TCP خودش را روی پورت 9000 راه‌اندازی می‌کند
  5. آماده دریافت اتصال از دیگران است
```

### مرحله 3: مشاهده کاربران آنلاین

```
علی دستور "peers" را می‌زند:
  1. درخواست به STUN Server می‌فرستد
  2. لیست کاربران آنلاین را دریافت می‌کند
  3. می‌بیند: رضا، مریم، حسین آنلاین هستند
```

### مرحله 4: برقراری ارتباط مستقیم

```
علی می‌خواهد با رضا چت کند:
  
  1. علی: "connect رضا"
     └─> از STUN Server آدرس رضا را می‌گیرد
     └─> مستقیماً به آدرس رضا متصل می‌شود (TCP)
  
  2. رضا: اتصال علی را می‌پذیرد
  
  3. حالا ارتباط مستقیم برقرار است! ✅
```

### مرحله 5: چت کردن

```
علی: "send رضا"
  You: سلام رضا!
  └─> پیام مستقیماً از علی به رضا ارسال می‌شود (بدون عبور از سرور)

رضا در برنامه‌اش می‌بیند:
  [علی → You]: سلام رضا!

رضا: "send علی"  
  You: سلام علی! حالت چطوره؟
  └─> پیام مستقیماً به علی می‌رسد

علی می‌بیند:
  [رضا → You]: سلام علی! حالت چطوره؟
```

### مرحله 6: ارسال فایل

```
علی: "file رضا"
File path: document.pdf

  1. فایل خوانده می‌شود
  2. مستقیماً به رضا ارسال می‌شود
  3. رضا فایل را در پوشه downloads ذخیره می‌کند
  
رضا می‌بیند:
  [✓] Received file 'document.pdf' (2.5 MB) from علی
  [→] Saved to: downloads/document.pdf
```

## 🐳 استفاده از Docker و Redis و ارسال فایل در چت

از این دو تکنولوژی و همینطور قابلیت ارسال فایل در چت به عنوان بخش امتیازی این پروژه پیاده سازی شده است
### نحوه استفاده:

```bash
# راه‌اندازی Redis با Docker
docker-compose up -d redis

# حالا STUN Server می‌تواند به Redis متصل شود
# اطلاعات کاربران در Redis ذخیره می‌شوند
```

## 🚀 راه‌اندازی کامل سیستم

### گام 1: راه‌اندازی Redis
```bash
docker-compose up -d redis
```

### گام 2: راه‌اندازی STUN Server
```bash
cd apps/stun-server
python src/main.py
```
سرور روی `http://localhost:8000` اجرا می‌شود

### گام 3: راه‌اندازی کاربر اول
```bash
cd apps/peer-node
# فایل .env را ویرایش کنید:
# PEER_USERNAME=علی
# PEER_PORT=9000

python src/main.py
```

### گام 4: راه‌اندازی کاربر دوم (در ترمینال جدید)
```bash
cd apps/peer-node
# فایل .env را ویرایش کنید:
# PEER_USERNAME=رضا
# PEER_PORT=9001

python src/main.py
```

### گام 5: برقراری ارتباط
```bash
# در ترمینال علی:
> peers          # مشاهده لیست
> connect رضا    # اتصال به رضا
> send رضا       # شروع چت
You: سلام!
exit
> file رضا       # ارسال فایل
> quit          # خروج
```

## 📊 نمودار جریان کلی

```
┌─────────────┐
│   کاربر 1   │
│   (علی)     │
└──────┬──────┘
       │
       │ ثبت نام
       ↓
┌──────────────────┐      ┌─────────┐
│  STUN Server     │◄────►│  Redis  │
│  (FastAPI)       │      └─────────┘
└──────────────────┘
       ↑
       │ ثبت نام
       │
┌──────┴──────┐
│   کاربر 2   │
│   (رضا)     │
└─────────────┘

پس از ثبت نام:

┌─────────────┐                    ┌─────────────┐
│   کاربر 1   │◄──── TCP ─────────►│   کاربر 2   │
│   (علی)     │   ارتباط مستقیم    │   (رضا)     │
└─────────────┘                    └─────────────┘
```

## ✨ ویژگی‌های مهم

✅ **ارتباط مستقیم**: پیام‌ها مستقیماً بین کاربران رد و بدل می‌شود، نه از طریق سرور

✅ **چند کاربره**: هر کاربر می‌تواند همزمان با چندین نفر چت کند

✅ **ارسال فایل**: امکان ارسال فایل‌های مختلف

✅ **مدیریت خطا**: اگر ارتباط قطع شود، به کاربر اطلاع داده می‌شود

✅ **استفاده از Docker**: راه‌اندازی آسان Redis

✅ **قابل تنظیم**: تمام تنظیمات از طریق فایل `.env` قابل تغییر است

## 🔧 پیکربندی

### STUN Server (.env)
```env
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
```

### Peer Node (.env)
```env
PEER_USERNAME=نام_کاربری_شما
PEER_IP=0.0.0.0
PEER_PORT=9000
STUN_SERVER_URL=http://localhost:8000
```

## 🎓 خلاصه

1. **STUN Server** = دفترچه تلفن (آدرس همه را نگه می‌دارد)
2. **Peer Node** = تلفن شما (با دیگران تماس می‌گیرید)
3. **Redis** = حافظه سریع برای نگهداری دفترچه تلفن
4. **Docker** = ابزار راحت برای اجرای Redis

هر کاربر **هم می‌تواند تماس بگیرد** (Client) و **هم می‌تواند تماس دریافت کند** (Server).
